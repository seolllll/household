# TASK

## 2026-09-14

### 환경/DB
- `.env.local`이 없어진 원인 확인: 이 작업 디렉토리가 GitHub에서 새로 클론된 상태라, `.gitignore`로 추적 제외되는 `.env.local`이 애초에 딸려오지 않았던 것. `.env.local.example`을 템플릿으로 재작성.
- Supabase 연결 오류 수정: `NEXT_PUBLIC_SUPABASE_URL`에 `/rest/v1/`가 잘못 붙어 있던 것 제거 (base URL만 넣어야 함).
- "카테고리가 없습니다" 오류의 원인이 RLS(Row Level Security)였음을 확인: SQL Editor(관리자 권한)로는 데이터가 보이지만 앱은 `anon` 키로 접근하기 때문에, `anon` 롤에 대한 SELECT 정책이 없으면 에러 없이 빈 배열만 반환됨. 해결책(RLS 비활성화 또는 anon 허용 정책 추가)을 안내함.

### 정산 화면 (일일/주간/월말)
- 월말정산에 있던 이전/다음 이동 UI 패턴을 앱 전체에 통일:
  - 주간정산 상세보기(`/weekly/[week]`)에 이전주/다음주 화살표 추가 (월 경계를 넘어가도록 처리).
  - 주간정산 목록(`/weekly`)도 지금 달에 고정되어 있던 것을 이전/다음 달 이동 가능하도록 수정.
  - 일일정산(`/`)을 "전체 내역 리스트"에서 "월 단위 + 날짜별 그룹 리스트" 구조로 변경, 상단에 월 이동 버튼 추가.
- 주간정산 상세보기의 "수입" 카드를 "남은 생활비"로 교체 (해당 월 1일부터 그 주 마지막 날까지의 누적 지출 기준 예산 잔액).
- 바텀 내비게이션 "일일정산" 아이콘을 `HomeIcon` → `NotebookPenIcon`으로 변경.

### 통계 탭
- 신규 섹션 추가: 기간별 지출 추이(주별/월별 토글 + 라인 차트), 수입 대비 지출 비율(도넛 게이지), 전월/전주 대비 증감률.
  - 차트는 기존에 쓰던 `recharts`만 재사용, 데이터 조회도 기존 `fetchTransactions`/`fetchMonthlyBudget`만 재사용 (신규 fetching 로직 없음, 전체 기간 한 번만 조회 후 클라이언트에서 집계).
  - 로딩 스켈레톤용 `Skeleton` 컴포넌트 신규 추가.
- 이후 요청에 따라 "카테고리별 예산" 섹션과 "예산 대비 실제 지출 진행률" 섹션은 삭제 (관련 미사용 코드도 함께 정리).

### 거래 추가/수정/삭제
- `queries.ts`에 `updateTransaction`, `deleteTransaction` 추가.
- `QuickAddModal`을 확장해서 `transaction` prop을 넘기면 "수정" 모드로 동작 (값 프리필 + `updateTransaction` 호출). 항목별로 `key`를 다르게 줘서 모달을 리마운트시키는 방식으로 상태 초기화 처리.
- 일일정산 리스트: 항목을 탭하면 수정/삭제 버튼이 펼쳐지도록 함. 삭제는 확인 단계 없이 즉시 삭제.

### 날짜 선택 UI
- 네이티브 `<input type="date">`가 Windows+Chrome 한국어 로케일에서 요일 표시가 빈 괄호 `( )`로 깨지는 문제 발견.
- TOAST UI Date Picker(`tui-date-picker`)로 교체: `src/components/ui/date-picker-input.tsx` 신규 작성.
  - SSR 시 `window is not defined` 크래시 → `next/dynamic({ ssr: false })`로 해결.
  - 캘린더 아이콘(SVG) 클릭 시 `elem.className.split is not a function` 크래시 → 아이콘에 `pointer-events-none` 추가로 해결 (SVG의 `className`은 문자열이 아닌 `SVGAnimatedString`이라 라이브러리 내부 클래스 처리 로직이 깨졌던 것).
  - 팝업 캘린더가 input 아래가 아니라 위로 뜨도록 `globals.css`에 위치 오버라이드 CSS 추가.

## 2026-09-15

### 환경
- `Module not found: Can't resolve 'tui-date-picker'` 오류 확인: `package.json`/`package-lock.json`에는 있지만 `node_modules`에 실제 설치가 누락된 상태였음. `npm install`로 해결.

### 카테고리 추가/수정/삭제 설정 기능
- `QuickAddModal`에 뷰 전환(`form` / `manage`) 방식으로 구현 (모달 위에 모달을 띄우는 대신 같은 Dialog 안에서 전환, 뒤로가기 버튼으로 복귀).
  - 카테고리 라벨 옆 설정(톱니바퀴) 아이콘 → 카테고리 관리 화면. 목록의 항목을 탭하면 그 칸 자체가 입력창 + 수정/삭제 버튼으로 바뀜(이름 중복 표시 없이 한 칸에서 수정).
  - 하단에 새 카테고리 추가 입력창.
- `queries.ts`에 `createCategory`, `updateCategory`, `deleteCategory` 추가.
- 카테고리 삭제는 소프트 삭제로 구현: `categories`에 `is_active` 컬럼 추가(Supabase에서 직접 실행), `deleteCategory`는 `is_active = false`로만 변경. 과거 거래는 FK 그대로 유지되고 정상 조회됨.
  - `fetchCategories`는 `is_active = true`만 조회.
  - `createCategory`는 동일 이름+타입의 비활성 카테고리가 있으면 새로 만들지 않고 재활성화(같은 id 재사용, 중복 생성 방지).

### 주간정산
- `RemainingBudgetBar` 컴포넌트 신규 추가: 남은 생활비 비율만큼 채워지고 지출이 늘수록 공백이 커지는 바(월말정산의 지출 채움 방향과 반대). `/weekly` 목록의 각 주차 박스 안에 적용.
- `/weekly` 목록: "상세보기" 버튼 제거, 박스 전체를 클릭하면 상세보기로 이동하도록 변경. 대신 박스를 눌러 펼치던 카테고리별 지출 내역(인라인) 기능은 제거.
- `/weekly/[week]` 상세보기: 제거된 카테고리별 지출 내역을 아코디언(기본 펼침, 화살표로 접기/펼치기)으로 이동해 추가.
- `SummaryCards`에 `lastCard` override prop 추가(미지정 시 기존처럼 `잔액 = 수입 - 지출`, 월말정산 등 다른 화면은 영향 없음).
- `/weekly/[week]` 카드 순서/의미 수정: 잔액(지난주까지 남은 생활비) → 지출(이번 주) → 남은 생활비(이번 주 반영 후) 순서로, 잔액 - 지출 = 남은 생활비가 되도록 계산.

### 캘린더/일일정산 화면 통일
- 캘린더(`/calendar`)와 일일정산(`/`)만 다른 화면과 달리 `Card` 없이 맨 리스트(`ring-1 ring-border`)로 되어 있던 것을 발견 → 주간/월말/통계와 동일하게 `Card`(`border-primary/30 bg-white`)로 감싸도록 통일.
- 캘린더 히트맵이 `bg-red-100~400` 하드코딩 색상을 쓰던 것을 디자인 시스템 색상 기반 `bg-expense/15~80` 투명도 스케일로 교체. 선택된 날짜 표시도 `ring-foreground`(검정) → `ring-primary`(보라)로 통일(버튼/FAB 선택 상태와 일관성).
- 참고: `--card` 베이지 컬러는 이제 앱 어디에서도 안 쓰임(모든 `Card`가 흰 배경+보라 테두리로 통일된 상태). 관련 기록이 낡아 있던 디자인 메모리도 함께 갱신.

### 카테고리 색상 자동 배정
- 새 카테고리 생성 시 색상이 지정되지 않아 항상 회색(`#888780` fallback)으로 표시되던 문제 확인.
- `createCategory`(`queries.ts`)에 8색 팔레트(`dataviz` 스킬의 검증된 카테고리컬 팔레트 재사용)를 추가. 같은 타입(수입/지출) 내에서 기존에 안 쓰인 색을 우선 배정하고, 팔레트가 소진되면 랜덤 재사용.

### 데스크탑(1920x1080) 반응형
- 전 화면이 `max-w-xl`(576px) 고정 모바일 레이아웃이라 큰 화면에서 좌우 여백이 과도했던 문제 확인.
- 멀티컬럼 재배치 대신 컨테이너 폭·폰트·카드 크기만 `lg:`(1024px~) 브레이크포인트에서 비례 확대하는 방향으로 사용자 확인 후 진행.
- `<main>` 컨테이너: `max-w-xl` → `lg:max-w-4xl`, 패딩/간격도 확대(6개 화면 공통 패턴). `BottomNav`도 동일 폭으로 정렬.
- 공용 `Card`/`CardTitle`, `Accordion` 컴포넌트에 `lg:` 폰트·패딩 확대 추가(이 컴포넌트들만 고치면 모든 화면의 카드가 같이 커짐). 개별 페이지의 제목/라벨/리스트 텍스트는 상속이 안 돼서 각각 `lg:text-*`를 붙임.
- recharts 차트 컨테이너 높이 확대(`h-48` → `lg:h-64`), 수입 대비 지출 도넛은 반지름을 px 고정값 대신 `%`로 바꿔 컨테이너 크기(`lg:size-44`)에 비례하도록 수정.

### 일일정산 엑셀 업로드
- 사용자 제공 `test.xlsx` 양식(1행 헤더, 2행부터 A~C=수입/날짜·금액·카테고리, D~G=지출/날짜·카테고리·금액·메모가 한 행에 나란히) 분석 후 `src/lib/import-excel.ts` 신규 작성. 양식이 추후 바뀔 걸 감안해 컬럼 매핑을 이 파일 하나에만 격리.
- `xlsx`(SheetJS) 의존성 추가. npm 배포 최신 버전(0.18.5)에 High 취약점(프로토타입 오염/ReDoS, 수정판 없음)이 있어서 SheetJS 자체 CDN이 배포하는 패치판(0.20.3, API 동일)으로 설치.
- `createCategory`(`queries.ts`)가 이제 생성/재활성화된 카테고리 id를 반환하도록 수정(기존 호출부 영향 없음) — 업로드 중 카테고리가 없으면 즉시 만들고 그 id로 거래를 추가하기 위함.
- 일일정산(`/`) 헤더에 업로드 버튼 추가: 파일 선택 → 파싱 → 없는 카테고리 자동 생성(랜덤 색상) → 거래 일괄 추가 → 목록 새로고침.
- 실제 파일로 테스트하다가 버그 발견: SheetJS `cellDates`가 날짜 셀을 로컬 타임존 `new Date(y,m,d)`로 만드는데 UTC getter로 읽어서 날짜가 하루 밀리던 문제 → 기존 `toDateKey`(로컬 getter)로 통일해 수정. 테스트 중 생성된 데이터는 모두 정리함.

### 업로드 로딩/완료 모달
- 업로드 버튼 아이콘이 처리 중엔 회전 스피너(`Loader2Icon` + `animate-spin`)로 바뀌고 버튼 비활성화.
- 결과는 인라인 텍스트 대신 기존 `Dialog`(수정/추가 모달과 동일 컴포넌트) 재사용해서 "N건 추가됨" 모달로 표시.

### 캘린더 날짜별 수입/지출
- 처음 요청은 "캘린더 각 날짜 칸에 수입/지출 금액 표시"였으나, 기존 지출 히트맵과의 관계 등을 논의하다 사용자가 방향을 바꿔서 "선택한 날짜의 수입/지출 합계를 하단 목록에 표시"로 확정.
- 캘린더(`/calendar`) 하단 "선택 날짜 거래 내역" 카드의 리스트 위에 그 날짜의 수입/지출 합계 요약 줄 추가(`text-income`/`text-expense`). 히트맵(배경색)은 그대로 유지.

## 2026-09-16

### 일일정산 화면
- 상단에 전체 수입/전체 지출/잔액 요약 박스(`SummaryCards`) 추가.

### 일일정산 엑셀 업로드 양식 변경
- 기존 test.xlsx(A~G 단순 표) 양식과 전혀 다른 "월간 리포트" 형식(test2.xlsx 계열) 파일로 교체 요청.
- `src/lib/import-excel.ts` 재작성: 실제 거래는 A~D(수입: 날짜/분류/내용/금액), F~I(지출: 날짜/분류/내용/금액)에서 파싱. P/R열(그 달 수입/지출 분류명 전체 목록 — 거래가 없어 합계 0인 분류도 포함, Q/S열 합계 숫자는 무시)을 읽어 거래가 없어도 카테고리를 미리 생성.
- 실제 파일로 검증: 수입 7건/지출 29건, 합계가 시트 자체 요약과 정확히 일치 확인.
- K~N열(저축 및 투자)은 이번 범위에서 제외, 추후 논의 예정으로 보류.

### 주간정산 - 고정비/특별비 제외
- 주간정산에 집계되는 지출에서 "고정지출", "특별비1", "특별비2(여행비)" 분류를 제외하도록 `isWeeklyBudgetExpense` 헬퍼(`src/lib/week.ts`) 추가.
- 주차 목록/상세 페이지의 지출 합계, 남은 생활비, 카테고리별 분해, 요일별 차트, 날짜별 드릴다운까지 전부 이 헬퍼 하나로 필터링해 일관성 유지.

### 월말정산 전면 개편
- test2.xlsx(월간 결산 리포트 양식) 분석 후 수입/고정지출/변동지출/자산현황/다음달 예산계획 구조로 전면 재작성하기로 결정.
- DB: 있었지만 안 쓰이던 `budgets`(category_id, month, amount) 테이블을 재활용해 `reason`(오차원인)/`feedback`(피드백) 컬럼 추가. 신규 `asset_items`(자산 항목 목록 10개 시딩: 유동성자산-투자/저축/현금, 비유동성자산-부동산, 설미, 대출)와 `asset_snapshots`(월별 금액+메모) 테이블 추가.
- 기존 "카테고리별 지출"(전월 대비 %증감 뱃지) 아코디언과 "이번 달 예산 진행률" 섹션은 삭제, 아래 섹션으로 교체:
  - 수입/고정지출: 엑셀 표 형태(`BudgetExcelTable`) — 세부항목|예산|실제|오차|오차원인|피드백. 예산 칸은 평소엔 통화 표기 텍스트, 클릭하면 숫자 입력으로 전환(주간정산 생활비 입력과 같은 패턴). 실제 금액 0인 행은 표에서 자동으로 숨김.
  - 변동지출: `BudgetGroupedExcelTable` — 분류|세부항목|예산|사용금액|오차|오차원인|피드백. `categories.report_group` 컬럼으로 여러 카테고리를 하나의 분류로 묶어 소계("OO총계") 행 표시(그룹에 2개 이상 모일 때만), 맨 아래 총합계. `report_group`이 비어있으면 카테고리 이름 자체가 분류명(단독 표시, 소계 없음) — 지금은 전부 비어있어서 그룹핑 미적용 상태.
  - 자산현황: 그룹>서브그룹>항목 트리, 전월 대비 오차 자동 계산(저장값이 아니라 항상 클라이언트에서 계산).
  - 다음달 예산계획: 고정지출/변동지출 각 카테고리별로 다음달 예산을 미리 입력(같은 `budgets` 테이블, month=다음달 키로 저장).
- 고정지출 세부항목 시도착오: 처음엔 "고정지출" 하나였던 카테고리를 계비/어린이보험/통신비/실비보험/주담대원리금 5개의 실제 카테고리로 분리하는 방식으로 구현했다가, 사용자 의도와 달라 롤백(생성했던 카테고리 5개 삭제, 참조 거래 없음 확인 후 진행).
  - 대신 `budgets`에 `label` 컬럼 추가(카테고리 안에서 세부항목을 구분하는 용도, 전체 카테고리 예산일 땐 빈 문자열)하고 unique 제약을 `(category_id, month)` → `(category_id, month, label)`로 변경. "고정지출" 카테고리는 하나로 유지한 채, 거래 메모 텍스트가 5개 세부항목명과 정확히 일치하는 것끼리 묶어서 세부항목 행으로 표시. 일치하지 않는 메모는 "기타" 행으로 묶어 합계가 항상 실제 카테고리 총액과 맞도록 처리.
  - 참고: 실제 8월 데이터는 메모가 "8월계비"/"농협손해보험"/"휴대폰요금"처럼 세부항목명과 다르게 적혀 있어 5개 중 2개(실비보험/주담대원리금)만 정확히 매칭되고 나머지는 "기타"로 묶임 — 사용자가 "이 부분은 추후에 수정"하겠다고 보류.

### 예산계획 탭 신규 추가
- 예산 "입력"은 전부 예산계획 탭으로 모으고, 월말정산은 조회 전용(오차원인·피드백만 편집 가능)으로 전환하기로 결정.
- 신규 라우트 `/budget-plan/[month]` (월말정산처럼 이전/다음 달 이동 가능). 섹션: 생활비(월 총액, `/weekly`에서 이관) · 주간별 예산 · 수입/고정지출/변동지출 계획(모두 `BudgetReviewTable` showActual=false 재사용).
- 주간별 예산은 일일정산과 동일한 add-modal 방식(`WeeklyBudgetItemModal`: 카테고리+금액+메모)으로 항목을 여러 개 추가하는 리스트 — 그 주 예산은 항목들의 합계(파생값, 별도 저장 안 함). 신규 테이블 `weekly_budget_items`(week_start, category_id, amount, memo). 계획용 목록이며 실제 거래(transactions)와는 무관. 항목을 탭하면 수정/삭제 버튼이 펼쳐짐(일일정산 리스트와 동일 인터랙션).
- 월말정산: "다음달 예산 계획" 아코디언 제거, 수입/고정지출/변동지출 표의 예산 칸을 읽기전용 텍스트로 전환(오차원인·피드백 입력은 유지). 카테고리·예산 행 빌드 로직(`buildRows`, `buildFixedExpenseLabelRows`)은 월말정산·예산계획 탭이 공유하도록 `src/lib/budget-rows.ts`로 분리.
- `/weekly` 목록: 생활비 입력 UI 제거(읽기전용, 입력은 예산계획 탭에서만), "실제 / 목표" 참고 표시는 그 주 `weekly_budget_items` 합계 기준.
- BottomNav에 예산계획 탭 추가 (월말정산 다음 순서, `PiggyBankIcon`).
- 새 테이블 만들 때마다 RLS 때문에 저장이 401로 막히는 문제가 반복됨 → 앞으로 create table SQL을 줄 때 항상 RLS disable/grant(또는 permissive policy) SQL을 같이 준다는 규칙을 메모리에 저장(사용자 명시적 요청).

## 2026-09-17

### 환경
- `Module not found: Can't resolve 'xlsx'` 오류 확인: 이전 tui-date-picker 때와 동일 원인(`package.json`엔 있지만 `node_modules`에 실제 설치가 누락). `npm install`로 해결.

### 주간정산 - 카테고리별 지출에 예산 비교 추가
- `/weekly/[week]` 카테고리별 지출 섹션에 그 주 `weekly_budget_items`(예산계획 탭 입력) 카테고리별 합계를 "예산 X / 지출 Y" 형식으로 함께 표시. 지출이 예산 초과면 빨간색(`text-expense`), 이하면 초록색(`text-income`).
- `CategoryBarBreakdown`의 `CategoryBarItem`에 `budget` 필드 추가.

### 예산계획 탭 - 수입계획/고정지출계획에 추가·수정·삭제 기능
- 수입계획: 헤더 + 버튼으로 새 카테고리 추가, 행을 펼치면 예산명 수정 + 삭제 가능. 실제 `categories` 테이블과 연동되어 앱 전체(카테고리 선택 등)에 그대로 반영됨 — 사용자 요청으로 화면에 안내 문구 표시.
- 고정지출계획: 코드에 하드코딩돼 있던 5개 세부항목(계비/어린이보험/통신비/실비보험/주담대원리금)을 신규 `budget_labels` 테이블로 옮겨 완전히 관리 가능하게 만듦(추가/이름수정/삭제). 이름 변경 시 그 라벨로 저장된 과거 `budgets.label` 기록도 새 이름으로 함께 갱신(연결 끊김 방지).
- `BudgetReviewTable`에 `manage` prop(onRename/onDelete) 추가. 월말정산은 별도 컴포넌트(`BudgetExcelTable`/`BudgetGroupedExcelTable`)라 영향 없음.
- 이름 수정용 별도 "수정" 버튼 제거 — 하단 "저장" 버튼 클릭 시 예산 금액과 함께 한 번에 저장. "삭제" 버튼은 "저장" 버튼 옆으로 위치 이동.
- 새 `budget_labels` 테이블도 RLS 정책(anon 허용) 함께 안내.

### 지출/수입 추가·수정 모달 - 고정지출 세부항목(2뎁스) 선택
- `QuickAddModal`에서 카테고리로 "고정지출"(`report_group` 기준)을 선택하면 그 카테고리 칩 바로 아래 줄에 저장된 세부항목(`budget_labels`) 칩 목록이 나타남(선택된 카테고리 칩 뒤에 `w-full` 블록을 끼워 넣어 flex-wrap이 강제 줄바꿈되도록 처리). 클릭하면 메모 필드에 그 이름이 채워짐(다시 클릭 시 해제) — 메모와 세부항목명이 일치해야 월말정산·예산계획 집계에 매칭되는 기존 규칙을 그대로 활용.
- 2뎁스 선택은 완전히 선택사항(카테고리만 선택해도 저장 가능). 수정 모드에서는 기존 메모가 세부항목명과 일치하면 자동으로 칩이 선택 표시됨.

### 예산계획 탭 - 변동지출계획을 항목 리스트로 전면 개편
- 기존 카테고리별 단일 예산 입력(`BudgetReviewTable`)을 주간별 예산과 같은 add-modal 방식으로 교체. 신규 `variable_budget_items` 테이블(month, category_id, amount, memo) + `VariableBudgetItemModal`/`VariableBudgetItemList` 컴포넌트 추가.
- 표시 형식: 카테고리(분류)별로 묶어서 개별 항목을 나열하고 그 아래 "OO 총계"를 항목 개수와 무관하게 항상 표시, 목록 맨 아래엔 전체 총계. 총계 줄에도 카테고리 색상 점 표시.
- 월말정산과의 연동(사용자 확인 후 진행): 항목 추가/수정/삭제 때마다 해당 카테고리의 합계를 `budgets.amount`에 자동 동기화(오차원인·피드백은 기존 값 유지) — 월말정산의 변동지출 "예산" 칸(조회 전용)이 이 목록을 그대로 반영하도록 함.

### 스타일 정리
- `BudgetReviewTable`의 `tabular-nums`를 라벨 포함 wrapping span이 아니라 숫자 span에 직접 적용하도록 재구성(변동지출계획 리스트와 동일 컨벤션).
- 수입계획/고정지출계획에서 "예산" 단어 제거(숫자만 표시), 총계 표시도 변동지출계획과 동일한 두 span(`총계` + `tabular-nums` 금액) 구조로 통일.

### 월말정산 변동지출 - 항목 단위 리뷰로 개편
- 기존엔 변동지출을 카테고리 단위로만 집계해서 보여줬는데(`BudgetGroupedExcelTable`), 예산계획 탭의 "변동지출 계획" 개별 항목(`variable_budget_items`)을 그대로 가져와 항목 단위로 보여주도록 전면 교체. 신규 `VariableBudgetItemReviewTable` 컴포넌트, 컬럼 순서는 항목|예산|사용금액|오차|오차원인|피드백. 더 이상 안 쓰이는 `BudgetGroupedExcelTable`은 삭제.
- 항목별 "사용금액"은 그 항목의 memo와 정확히 일치하는 거래만 합산(고정지출 세부항목과 동일한 매칭 방식) — memo가 다르면 0으로 표시될 수 있는 트레이드오프 있음(사용자 확인 후 진행).
- 오차원인/피드백을 항목별로 독립 입력하기로 결정(카테고리 단위 공유 대신) → `variable_budget_items`에 `reason`/`feedback` 컬럼 신규 추가(Supabase에서 직접 실행).
- 버그 수정: 카테고리에 항목이 1개뿐이면 "OO 총계" 행이 안 나오던 것 → 항목 개수와 무관하게 항상 표시하도록 수정(변동지출계획 리스트와 동일 컨벤션 재확인).

### 변동지출 계획 - 상세 메모 추가
- `variable_budget_items`에 `detail_memo` 컬럼 추가(Supabase에서 직접 실행). 실거래 매칭에 쓰이는 `memo`와는 분리된 순수 참고용 텍스트(예: "스타벅스 위주, 주 3회").
- 예산계획 탭 항목 리스트와 월말정산 리뷰 테이블 둘 다에 항목명 우측에 작은 회색 글씨로 표시.

### 지출 추가 모달 - 변동지출도 2뎁스 세부항목 선택 지원
- 기존엔 "고정지출" 카테고리만 `budget_labels`(월 무관 고정 목록)로 2뎁스 칩을 보여줬는데, 그 외 지출 카테고리는 거래 날짜가 속한 달의 `variable_budget_items` memo(월별 계획, 중복 제거)를 2뎁스 칩으로 보여주도록 일반화. 두 소스가 같은 UI 패턴(칩 클릭 → 메모 채움, 완전히 선택사항)을 타도록 `SubItemOption` 공용 타입으로 정리.
- 오늘 날짜가 아니라 선택된 거래 날짜 기준으로 달을 계산해서 조회 — 과거 날짜로 소급 입력해도 그 달 계획이 정확히 뜨도록.

### 모달 닫힐 때 입력값 초기화
- `VariableBudgetItemModal`/`WeeklyBudgetItemModal`을 저장 없이 닫았다가 "새로 추가"로 다시 열면 이전 입력값이 남아있던 버그 발견(부모의 `key` 리마운트가 "새 항목 추가"를 연달아 열 때는 키가 안 바뀌어서 안 먹힘) → `QuickAddModal`과 동일하게 Dialog가 닫히는 순간(`onOpenChange(false)`) 내부 상태를 초기화하도록 수정.

### 월말정산 변동지출 - 예산계획에 없는 지출도 자동집계
- 기존엔 그 달 `variable_budget_items`(예산계획)에 있는 항목만 표시돼서, 항목 메모와 정확히 매칭 안 되는 지출이나 예산계획에 아예 없는 카테고리의 지출은 화면에서 누락되고 있었음.
- `VariableBudgetItemReviewTable` 개편: 카테고리별로 (그 카테고리 실제 거래 총액 − 항목들에 매칭된 금액) 잔여분이 있으면 "기타"(항목이 하나도 없는 카테고리는 카테고리명) 자동집계 행을 추가. 신규 DB 테이블/컬럼 없이 고정지출 "기타" 행과 동일하게 `budgets` 테이블의 `label="기타"` 슬롯을 재사용(`upsertBudget`)해서 오차원인·피드백도 저장 가능하게 함.
- 카테고리 소계·총합계에도 자동집계분을 포함해서 실제 지출 총액과 항상 맞도록 계산.

### 월말정산 - 오차원인/피드백 입력칸 UI 개선
- 신규 `ReviewInputCell` 컴포넌트: 값이 채워지면 테두리를 숨겨 텍스트처럼 보이게 하고(포커스하면 다시 표시), 피드백은 평소엔 말줄임표(`...`)로 잘려 보이다가 클릭하면 여러 줄 textarea로 확장.
- 수입/고정지출(`BudgetExcelTable`), 변동지출(`VariableBudgetItemReviewTable`)에 적용, 이후 자산현황 표에도 동일 적용.

### 월말정산 자산현황 - 아코디언 → 엑셀 표로 전면 개편
- 표 컬럼: 구분|내역|상세내역|지난달 자산|이번달 자산|오차|오차원인|피드백. 구분·내역이 연속으로 같으면 `rowSpan`으로 병합, 구분 경계마다 "OO 총계" 소계 행 + 맨 아래 총합계.
- 항목 추가 기능 신규 추가: 우측 상단 "+" 버튼 → 구분/내역/상세내역 3칸 인라인 입력(`createAssetItem`).
- 항목 수정/삭제 기능 추가: 상세내역 텍스트를 클릭하면 수정 폼(구분/내역/상세내역 + 저장/삭제/취소)이 펼쳐짐(`updateAssetItem`/`deleteAssetItem`, 삭제는 `categories`와 동일하게 `is_active=false` 소프트 삭제). 구분을 기존에 있는 값으로 바꾸면 그 구분 밑으로 자동으로 합쳐지도록 `resequenceAssetItems`가 전체 목록의 `sort_order`를 재배치.
- 버그 수정: `resequenceAssetItems`를 처음엔 행마다 순서대로 업데이트했는데, (1) 아직 안 옮겨진 다른 행과 순번이 잠깐 겹칠 수 있고 (2) 중간에 끊기면(브라우저 종료 등) 절반만 바뀐 채로 남는 문제로 실제 데이터의 `sort_order`가 꼬이는 걸 발견 → 임시 구간으로 옮겼다가 최종 위치로 옮기는 2단계 + 각 단계를 bulk upsert(요청 1~2번)로 처리하도록 수정하고 꼬였던 데이터도 복구.

### 월말정산 - 상단 요약카드 제거
- 상단 수입/지출/잔액 `SummaryCards` 제거(전용으로만 쓰이던 `income` 계산도 함께 정리).

### 엑셀 다운로드 기능 신규 추가
- 다운로드 버튼 위치 논의: 다운로드 대상이 일일/주간/월간 정산을 가로지르는 리포트성 기능이라 특정 CRUD 화면보다 조회 전용인 통계 탭이 적합하다고 판단, 통계 탭 헤더에 다운로드 아이콘 버튼 추가.
- `ExcelDownloadDialog` 신규 컴포넌트: 최근 12개월 체크박스(기본 이번달) + 일일/주간/월간정산 체크박스(기본 전체)로 선택 후 다운로드.
- `src/lib/export-excel.ts` 신규 작성 — 화면에 이미 있는 집계 로직을 그대로 재사용해 엑셀로 직렬화:
  - 일일정산 시트: 날짜순 거래 목록 + 수입/지출/잔액 합계.
  - 주간정산 시트: 주차별 지출/예산/남은 생활비 요약 + 주차별 카테고리 지출 내역(`isWeeklyBudgetExpense` 재사용).
  - 월간정산 시트: 수입/고정지출/변동지출/자산현황 4개 표를 월말정산 화면과 동일한 로직(`buildRows`/`buildFixedExpenseLabelRows`/`AssetReviewSection` 집계 방식)으로 구성.
  - 변동지출 집계용으로 `VariableBudgetItemReviewTable`의 `buildGroups`/`actualForItem`을 export해서 재사용(중복 구현 없음).
  - 시트명은 "O월 일일정산" 형식, 여러 해가 섞여 선택되면 자동으로 "YYYY년 O월 ..."로 전환해 이름 충돌 방지.
- Playwright로 실제 다운로드 흐름(다이얼로그 열기 → 기본값 그대로 다운로드) 검증, 콘솔 에러 없음과 생성된 xlsx의 시트/데이터 정확성 확인 완료.

### 엑셀 다운로드 - 실제 손으로 써오던 템플릿(`2026 가계부.xlsx`)에 맞춰 스타일링 전면 개편
- 기본 버전이 "너무 못생겼다"는 피드백 → 사용자가 실제 사용해온 엑셀 템플릿 파일을 분석해 구조/스타일을 재현하기로 함.
- 템플릿 분석 결과 주간정산 시트가 "카테고리별 주차 누적 표" + "주차마다 일~토 요일별 캘린더 그리드(그날 수입/지출 나열+합계)"로 이중 구성된 것을 확인 → 요일별 캘린더 그리드까지 완전 재현하기로 사용자 확인.
- 색상/테두리/병합 등 스타일을 쓰려면 기존 `xlsx`(SheetJS 커뮤니티 에디션)로는 불가능(쓰기 스타일링은 Pro 전용)함을 확인 → 무료로 스타일 쓰기가 가능한 `exceljs`를 신규 의존성으로 추가, `export-excel.ts`를 ExcelJS 기반으로 전면 재작성. 브라우저에서 다운로드 트리거는 `workbook.xlsx.writeBuffer()` → Blob → `<a download>` 클릭 방식으로 구현.
- 스타일은 템플릿의 정확한 hex 대신 앱 자체 CSS 변수(`--primary` 보라, `--income` 초록, `--expense` 빨강)와 기존 카테고리 팔레트에서 색을 골라 앱과 톤을 통일(수입=초록, 고정지출=파랑, 변동지출=보라, 자산현황=주황 헤더 + 오차 칸은 기존 `varianceClass` 로직과 동일하게 초록/빨강).
- 주간정산 캘린더 그리드: 같은 날 같은 카테고리에 거래가 여러 건이면 그만큼 행을 늘려 각 요일 칸에 하나씩 배치(템플릿과 동일 방식). 템플릿에 있던 "예산"/"이벤트"/"오차 내역" 행(그날그날 손으로 채우던 자유 메모성 데이터)은 앱 데이터 모델에 대응 항목이 없어 제외. 그리드 카테고리 행도 그 주에 실제 지출이 있는 카테고리만 표시(템플릿은 항상 전체 표시하지만, 5주치 반복 시 빈 행이 너무 많아져 가독성 위해 생략) — 반면 상단 누적표는 템플릿처럼 전체 지출 카테고리를 항상 표시.
- ExcelJS로 다시 읽어 값/스타일(fill·font·merge)이 의도대로 반영됐는지, Playwright로 실제 다운로드까지 재검증 완료.

### 엑셀 다운로드 - 일일정산 시트를 템플릿과 100% 동일 구조로 재작성
- 기존엔 앱 화면(날짜/구분/카테고리/금액/메모 단일 표) 구조로 만들었었는데, 사용자가 템플릿의 일일정산 시트(`26년8월`)와 완전히 동일한 레이아웃을 요청.
- 템플릿을 다시 정밀 분석(병합 범위 좌표까지 확인)해 그대로 재현: 2행 병합 제목("2026년 9월") → 전체 수입/전체 지출/저축 및 투자/잔여 금액 요약 4줄(값은 B:D 병합) → 수입/지출/저축 및 투자 3분할 섹션 헤더(파스텔 핑크/피치/블루, 각 4열 병합) → 전체 합계 재표시 줄 → 날짜/분류/내용/금액 4열짜리 표 3개(수입 A-D, 지출 F-I, 저축 및 투자 K-N) → 우측에 카테고리별 이번 달 합계 목록(수입 P-Q, 지출 R-S, 활성 카테고리 전체를 항상 나열).
- "저축 및 투자"는 앱에 아직 없는 데이터(추후 논의 예정 항목)라 구조(헤더)만 만들고 항상 0/빈 테이블로 유지.
- 컬러는 이 시트만 템플릿의 실제 파스텔 hex(연분홍/연복숭아/연하늘)를 그대로 사용(다른 시트는 계속 앱 자체 팔레트 유지 — 사용자가 이미 "일단 ㄱㅊ"로 확인함).
- ExcelJS로 재검증: 병합 좌표(`A1:N2`, `B3:D3` 등)와 셀 값이 템플릿 구조와 일치함을 확인.

### 통계 탭 - 라인차트 클릭 시 해당 기간 통계로 갱신
- 처음엔 라인차트 클릭 시 `/weekly/[week]`·`/monthly/[month]` 상세 화면으로 이동하도록 구현했으나, 사용자가 원한 건 페이지 이동이 아니라 하단 "수입 대비 지출 비율"/"전주·전월 대비 증감률" 카드가 클릭한 시점 기준으로 바뀌는 것이라는 피드백을 받고 방향 수정.
- `selectedIndex` state 추가(기본 `null` = 최신 버킷). 라인차트 클릭 시 그 버킷의 인덱스를 저장하고, 아래 두 카드는 항상 `buckets[selectedIndex ?? 마지막]` 기준으로 계산하도록 변경. 주별/월별 토글을 바꾸면 버킷 배열 자체가 달라지므로 선택 인덱스를 초기화.
- 선택된 지점은 도트를 크게+테두리로 강조 표시. recharts의 `LineChart onClick`이 주는 `activeTooltipIndex`가 숫자가 아니라 숫자 형태의 문자열로 내려오는 걸 Playwright로 실제 클릭해보고 발견해 `Number()`로 변환.

### 통계 탭 - 수입 대비 지출 비율은 월별 전용으로, 순자산 추이 섹션 신규 추가
- "수입 대비 지출 비율" 카드가 주별 모드에도 뜨던 걸, 수입은 대체로 월 단위로 들어와 주별로는 의미가 약하다는 피드백 → `period === "monthly"`일 때만 표시하도록 조건부 렌더링.
- 일일/주간/월말정산과 겹치지 않는 통계 후보로 순자산 추이/카테고리별 지출 비중/예산 소진율 추이를 제안했고, 순자산 추이로 결정. 최근 6개월치 `asset_snapshots`를 `asset_items.group_name` 기준으로 "유동성 자산"/"비유동성자산" 두 그룹으로 합산해 라인차트 2개로 표시(대출/설미 그룹은 이번 범위에서 제외, 요청받은 두 그룹만).
- 자산 데이터는 거래 내역과 별도 fetch라 로딩 상태를 분리(`assetLoading`), 두 그룹 모두 0인 달만 있으면 "자산현황 데이터가 없습니다"로 안내.

### 통계 탭 - 순자산 추이 → "투자 자산 추이"(유동성 자산 > 투자 내역별)로 범위 축소
- 유동성/비유동성 2개 라인 구성에 대해 "내역별로 보여주는 게 비효율적인가" 질문 → 개별 항목(6개) 전부 넣으면 모바일 폭에서 안 읽힐 수 있어 비효율적이라 답변, 대신 하위 구분(투자/저축/현금) 단위 분리를 제안.
- 사용자가 비유동성 자산은 빼고 "유동성 자산 > 투자" 내역(RS/해외주식/신한투자, 3개 계좌)만 각각 라인으로 보여달라고 확정.
- `asset_items`에서 `group_name === "유동성 자산" && subgroup === "투자"`인 항목만 추려 항목 개수만큼 동적으로 `<Line>`을 렌더링(항목 id를 데이터 키로 사용). 색상은 카테고리 팔레트에서 3개 고정 배정, 툴팁도 항목별로 동적 표시.

### 통계 탭 - 카테고리별 지출 비중 랭킹(Top 10) 추가
- "기간별 지출 추이" 그래프에서 선택한 기간(주/월) 기준으로 그 기간 지출을 카테고리별 합산 후 금액 내림차순 Top 10을 순위·비중(%)·비례 막대로 표시. 새 fetch 없이 이미 로드해둔 `transactions`를 재사용.
- 그래프 클릭 시 아래 통계가 갱신되는 기존 동작(수입 대비 지출 비율, 전주·전월 대비)에 이 카드도 자연스럽게 포함되도록 같은 `selectedBucket` 기준으로 계산.
- 카테고리가 없는 거래(`category` null)는 "미분류"로 묶어서 표시(앱 다른 화면의 관례와 동일).
- 처음엔 항목별 수평 막대(가로 바)로 비중을 표시했는데, 세로 컬럼차트로 바꿔달라는 요청 → recharts `BarChart`(카테고리색 `Cell`, X축은 순위 숫자만 표시)로 교체하고, 순위-카테고리명 매핑은 차트 아래 텍스트 목록으로 유지(카테고리명이 길어 X축엔 다 못 넣음).
- "수입 대비 지출 비율"과 마찬가지로 카테고리별 지출 비중도 주별 단위로는 의미가 약하다는 이유로 `period === "monthly"`일 때만 표시하도록 변경.

### 통계 탭 - 주별 통계 완전 삭제, 월별 통계만 남김
- 카드마다 "월별 데이터만 의미 있음"으로 조건부 숨김 처리를 반복하던 중, 아예 주별 통계 자체를 없애자는 결정 → `period`/`TrendPeriod`/주별·월별 토글 버튼/`weeklyBuckets`/`getRecentWeekRanges` 등 주별 관련 코드를 전부 제거하고 "기간별 지출 추이"를 "월별 지출 추이"로, `PeriodBucket`을 `MonthlyBucket`으로 정리.
- 그래프 클릭 시 선택된 월 기준으로 아래 카드들이 갱신되는 동작은 그대로 유지(이제 그 "기간"이 항상 월 단위).

## 2026-09-18

### 로그인 기능 추가
- 처음엔 Supabase Auth(`auth.users`) 기반으로 가구(household) 단위 멀티테넌시를 기획(신규 `households`/`household_members` 테이블, RLS 정책, `auth.uid()` 기반 INSERT 트리거)했으나, 구현 직전 "Supabase Auth를 쓰지 않고 직접 구현하고 싶다"는 방향 전환 요청을 받아 전면 재설계.
- 최종 구조: **모든 DB 접근을 Next.js 서버(Server Action)가 대행**하도록 변경. 브라우저는 더 이상 Supabase를 직접 호출하지 않으며, `NEXT_PUBLIC_SUPABASE_ANON_KEY`도 완전히 제거(서버 전용 `SUPABASE_SERVICE_ROLE_KEY`로 대체).
  - 계정은 자체 `app_users`(username/password_hash/household_id) 테이블로 관리(`household_members` 삭제, `auth.users` 미사용). 비밀번호는 `bcryptjs`로 해시.
  - 세션은 `jose`로 서명한 stateless JWT를 `httpOnly` 쿠키에 저장(Next.js 공식 인증 가이드와 동일 패턴). 신규 `src/lib/session.ts`(createSession/getSession/deleteSession/requireHouseholdId), `src/lib/supabase/admin.ts`(service role 클라이언트, `server-only`), `src/lib/auth-actions.ts`(login/logout Server Action).
  - `src/lib/queries.ts`의 기존 함수 33개 전부 `"use server"`로 전환하고, 각 함수 시작에서 `requireHouseholdId()`로 세션의 household_id를 읽어 모든 조회/삽입/수정/삭제에 명시적으로 필터링 — RLS 대신 이 서버 코드의 필터링 자체가 실제 보안 경계가 됨.
  - 로그인 화면은 이메일이 아니라 "아이디"(순수 텍스트, `@` 불필요)로 입력받음 — Supabase Auth를 안 쓰므로 이메일 형식 제약이 아예 사라짐.
  - `src/app/layout.tsx`(서버)가 세션을 먼저 확인해 `src/lib/session-context.tsx`로 내려주고, `src/components/app-shell.tsx`가 라우트 가드(미로그인 시 `/login`으로, 로그인 시 `/login` 접근하면 홈으로) 역할. 서버가 이미 로그인 여부를 알고 있어 로딩 깜빡임 없음.
  - DB: 신규 `households`/`app_users`, 기존 9개 테이블(`categories`/`transactions`/`budgets`/`monthly_budgets`/`weekly_budget_items`/`budget_labels`/`variable_budget_items`/`asset_items`/`asset_snapshots`)에 `household_id` 컬럼 추가 후 백필. RLS·트리거는 불필요(서버가 유일한 접근 경로이기 때문).
  - 계정 2개(`sjyeom`, `smchoi`) 생성, 서로 다른 `household_id`로 분리해 가구 간 데이터 격리 동작 확인.

### 성능 - Server Action 전환 후 렌더링 지연 원인 발견 및 수정
- 로그인 구조 전환으로 `queries.ts`의 모든 조회 함수가 Server Action이 된 이후, 월말정산을 비롯한 여러 화면의 렌더링이 눈에 띄게 느려지는 문제 발생.
- 원인 확인: Next.js 공식 문서에 "Server Function은 클라이언트→서버 요청을 한 번에 하나씩만 처리한다(dispatches and awaits them one at a time)"고 명시돼 있음. 화면에서 `Promise.all`로 여러 `fetch*`를 동시에 부르는 것처럼 짜여 있어도 실제로는 순차 왕복이 되는 것. 월말정산은 페이지 하나당 최대 10회, 엑셀 다운로드는 선택한 달 수만큼(최대 150회 이상) 순차 왕복이 발생하고 있었음.
- 해결: `queries.ts`에 페이지별 통합 조회 함수(`fetchWeeklyListPageData`/`fetchWeeklyDetailPageData`/`fetchMonthlyPageData`/`fetchBudgetPlanPageData`/`fetchInvestmentTrendData`/`fetchExportData`/`bulkImportTransactions`) 신규 추가 — 내부적으로는 기존 `fetch*` 함수를 그대로 `Promise.all`로 묶어 서버 안에서 병렬 처리(서버 내부 호출은 "한 번에 하나씩" 제약과 무관), 클라이언트는 왕복 1번만 하도록 `weekly`/`weekly/[week]`/`monthly/[month]`/`budget-plan/[month]`/`stats`/일일정산 엑셀 업로드/엑셀 다운로드(`export-excel.ts`) 전부 수정.

### 월말정산 자산현황 - 금액 입력칸 UI 개선
- 오차원인/피드백과 동일한 패턴으로, 금액도 값이 채워지면 테두리를 숨기고 `₩30,000` 형식으로 표시, 클릭(포커스)하면 다시 숫자 입력으로 전환되도록 수정(`asset-review-section.tsx`).

## 다음 작업 (예정)
- [ ] 변동지출 분류 묶음(`categories.report_group`) 값 채우기 — 지금은 전부 비어있어서 카테고리별 단독 표시 상태
- [ ] 저축 및 투자(K~N열) 업로드 반영 방식 논의
- [ ] 일일정산 파일 업로드 양식 수정
- [ ] 일일정산 업로드 시 "전체 삭제 → 삽입" 방식으로 변경
- [ ] 로그인 배포 전 비밀번호(현재 `1234` 등 테스트용) 강화, 가족 구성원 추가 계정 발급
- [ ] (추가 예정 항목 있음)
